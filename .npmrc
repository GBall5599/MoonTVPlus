# 使用国内镜像源，规避 registry.npmjs.org 在部分网络下的连接问题
registry=https://registry.npmmirror.com/
# 降低并发，避免受限网络/沙箱环境下的连接重置
network-concurrency=4
child-concurrency=2
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000